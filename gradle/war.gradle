apply plugin: "war"

bootWar {
    mainClassName = "nc.redstone.ticketresto.TicketRestoApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
